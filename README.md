# Diffusion_Elastic_Droplet
Toy version of the phase separation problem; the droplet-Gaussian bump interaction are calculated as a "soft version", depends on the overlap between droplet and Gaussians
